{
  "name": "@jericirenej/object-filter",
  "description": "Return an object with filtered properties based on a supplied list of filter keys. Exclude/include filtering options and recursive operation available.",
  "author": "Irenej Jeric",
  "version": "1.2.0",
  "license": "ISC",
  "main": "./dist/index.cjs",
  "exports": {
    "import": "./dist/index.mjs",
    "require": "./dist/index.cjs"
  },
  "types": "dist/index.d.ts",
  "type": "module",
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "test": "jest",
    "dev": "tsc-watch",
    "prepublishOnly": "npm run compile",
    "precompile":"npm run clean",
    "compile:mjs": "set NODE_ENV=esmodule && babel src --out-dir ./dist --extensions .ts --out-file-extension .mjs",
    "compile:cjs": "set NODE_ENV=commonjs && babel src --out-dir ./dist --extensions .ts --out-file-extension .cjs",
    "compile": "npm run compile:mjs && npm run compile:cjs && tsc --emitDeclarationOnly",
    "build": "tsc",
    "clean": "rm -rf dist/*"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/jericirenej/object-filter.git"
  },
  "bugs": {
    "url": "https://github.com/jericirenej/object-filter/issues"
  },
  "keywords": [
    "no-dependencies",
    "nodependencies",
    "zero-dependencies",
    "zerodependencies",
    "object",
    "object-filter",
    "javascript",
    "typescript",
    "filter",
    "object-trim",
    "trim"
  ],
  "prettier": {
    "arrowParens": "avoid"
  },
  "devDependencies": {
    "@babel/cli": "^7.17.10",
    "@babel/core": "^7.18.0",
    "@babel/preset-env": "^7.18.0",
    "@babel/preset-typescript": "^7.17.12",
    "@types/jest": "^27.5.0",
    "@types/node": "^17.0.31",
    "@typescript-eslint/eslint-plugin": "^5.22.0",
    "@typescript-eslint/parser": "^5.22.0",
    "babel-plugin-add-import-extension": "^1.6.0",
    "eslint": "^8.15.0",
    "jest": "^28.1.0",
    "ts-jest": "^28.0.1",
    "tsc-watch": "^5.0.3",
    "typescript": "^4.6.4"
  },
  "engines": {
    "node": ">=14.17.0"
  }
}
